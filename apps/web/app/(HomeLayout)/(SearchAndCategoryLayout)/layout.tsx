import { ReactNode } from "react";

const SearchAndCategoryLayout = ({ children }: { children: ReactNode }) => {
  return <div className="py-10">{children}</div>;
};

export default SearchAndCategoryLayout;
